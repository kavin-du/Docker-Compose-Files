### 1. Go to `localhost:8080` for `Adminer` login page
### 2. Choose `Postgres` from dropdown menu as the database 
### 3. Enter username and password
### 4. `Adminer` runs on port 8080 and `Postgres` runs on port `5432`
